# AGENTS.md —— AHK-Replay · AI 使用说明

通用「操作录制 + 循环回放」工具（AutoHotkey v2 单文件脚本，无需构建）。录制真人在任意网页/软件里的点击、滚轮、按键与粘贴，之后无限次按原节奏循环回放——对任何应用通用，因为回放的就是真人级系统输入事件。

## AI 的角色

录制和回放都由**用户在目标应用里真人完成**，AI 不代替用户发送鼠标键盘事件。AI 负责：启动脚本、指导用户录制、管理录制文件、调参、读日志排障、直接编辑 `.ahkr` 修正坐标。

## 启动与快捷键

- 前提：本机已安装 AutoHotkey **v2**（脚本声明 `#Requires AutoHotkey v2.0`）
- 启动：双击 `start.bat`（或直接运行 `AHK-Replay.ahk`），托盘出现绿色 H 图标
- 快捷键（用户按）：

| 键 | 功能 |
|---|---|
| F2 | 开始/结束录制（结束自动存入 `recordings/` 并询问回放次数） |
| F4 | 回放最近一次录制 |
| F5 | 从 `recordings/` 挑选任意历史录制回放 |
| F12 | 急停：回放中立即停止；平时按下退出脚本 |

## 指导用户录制时必说的要点

- 回放是**绝对坐标**：录制开始后目标窗口的位置和缩放不能变（保持最大化最稳）
- 录制的**最后一步**要让页面回到"能操作下一条"的状态（如聊天页点「下一个」、列表页向下滚动），否则每轮重复同一条
- 回放期间不要动鼠标键盘
- 每次 Ctrl+V 连同当时剪贴板内容一起存档，回放自动还原——录什么贴什么，不依赖外部文件
- 超过 2.5 秒的发呆自动压缩；每轮之间休息 `INTER_LOOP_MS`（默认 3 秒）

## AI 可直接做的事

- **调参数**：改 `AHK-Replay.ahk` 顶部配置区——`INTER_LOOP_MS`（轮间隔毫秒）、`MAX_WAIT_COMPRESS`（停顿压缩阈值毫秒），保存后重启脚本生效
- **微调录制**：`recordings/*.ahkr` 是纯文本（事件序列 + 坐标 + 剪贴板快照），可用编辑器直接改坐标/间隔后重放
- **排障**：读 `ahk-replay.log`（运行日志，自动追加）
- 提醒用户控制回放频率与总量，遵守目标网站服务条款，注意账号安全

## 文件与仓库

```
AHK-Replay.ahk    主脚本（含顶部配置区）
start.bat         双击启动
recordings/       录制存档（gitignore，含用户操作数据不入库）
ahk-replay.log    运行日志（gitignore）
```
